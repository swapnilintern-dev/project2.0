<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain-HTTP (cleartext) traffic ONLY to local development hosts so the
  app can reach the Express backend during development:
    - 10.0.2.2   : the host machine as seen from the Android emulator
    - localhost / 127.0.0.1 : on-device loopback
  All other domains still require HTTPS (cleartext denied by default on API 28+),
  so this does not weaken production security.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
